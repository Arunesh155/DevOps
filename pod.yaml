apiVersion: v1
kind: Pod
metadata:
  name: my-pod
  labels:
      app: my-web-app
      type: backend
spec:
  containers:
    - name: nginx-container
      image: arunesh2005/simpleapplication
      ports:
        - containerPort: 90
